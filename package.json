{
  "name": "fvgs.ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "http-server -a 127.0.0.1 -p 7890",
    "dist": "node-sass src/ -o dist/",
    "watch": "npm run dist && node-sass -w src/ -o dist/"
  },
  "dependencies": {
    "normalize.css": "5.0.0"
  },
  "devDependencies": {
    "http-server": "0.9.0",
    "node-sass": "3.10.1"
  }
}
